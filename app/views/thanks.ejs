<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('header') %>
    <!-- <script src="js/thanks.js"></script> -->
</head>

<body>
    <%- include('nav', navigation) %>
        <div class="container d-flex justify-content-center mt-5" id="thanks">
        <h2 id="thanks_text">
            <%- thanks %>
        </h2>
        </div>

        <div
        id="slogan-wrapper"
        class="d-flex justify-content-center align-items-center"
        style="min-height: 5vh;"
        >
        <h4 id="slogan" class="text-center">
            <%- slogan %>
        </h4>
        </div>

    <div class="container mb-5">
        <!-- <button type="" id="donate_again" onclick="navigateHomePage('<%= navigation.languageCode %>')"><%= donate_again || 'Donate again!' %></button> -->
        <button type="submit" class="button" id="donate_again" onclick="location.href='<%= process.env.APP_BASE_PATH %><%= navigation.languageCode %>/donate'"><%= donate_again || 'Donate again!' %></button>
    </div>

    <!--
    <div class="container mb-5">
        <a href='<%= process.env.APP_BASE_PATH %><%= navigation.languageCode %>/reward'><button type="" id="reward"><%= reward || 'Get your reward' %></button></a>
    </div>
    -->
    <%- include('footer') %>
</body>
</html>
