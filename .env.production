# Production Environment Configuration
# Domain configuration
DOMAIN=habit.wiwi.tu-dresden.de
SERVER_IP=141.76.16.16

# Let's Encrypt email for SSL certificate notifications and renewal reminders
ACME_EMAIL=felix.reinsch@tu-dresden.de

# Traefik Dashboard Authentication
# Generate with: echo $(htpasswd -nb admin your-password) | sed -e s/\\$/\\$\\$/g
# Default: admin / change-this-password
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$8EVjn/nj$$GiLUZqcbueTFeD23SuB6x0

# App Configuration
APP_BASE_PATH=/
NODE_ENV=production

# Fuseki API
FUSEKI_PATH=fuseki
DB_HOST=fuseki
DB_PORT=3030
DB_USER=admin
DB_PASSWORD=CHANGE_THIS_FUSEKI_PASSWORD
DB_PATH=hhh

# Translate API
LT_LOAD_ONLY=de,en,ja
LT_REQ_LIMIT=0
LT_DEBUG=false
LT_DISABLE_WEB_UI=false

# Database Passwords - CHANGE THESE!
ADMIN_PASSWORD=CHANGE_THIS_ADMIN_PASSWORD

# MongoDB Configuration
MONGO_HOST=mongo
MONGO_PORT=27017
MONGO_USER=admin
MONGO_PASSWORD=CHANGE_THIS_MONGO_PASSWORD
MONGO_DB=surveyjs
MONGO_AUTH_SOURCE=admin
MONGO_SERVER_SELECTION_TIMEOUT_MS=30000
MONGO_SOCKET_TIMEOUT_MS=30000

# Graph storage backend: 'fuseki' or 'neo4j'
GRAPH_BACKEND=neo4j

# Neo4j Configuration
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=CHANGE_THIS_NEO4J_PASSWORD

# reCAPTCHA Configuration
# IMPORTANT: Get your production keys from https://www.google.com/recaptcha/admin
# 1. Go to https://www.google.com/recaptcha/admin/create
# 2. Select reCAPTCHA v2 (or v3 depending on your implementation)
# 3. Add domain: habit.wiwi.tu-dresden.de
# 4. Replace the values below with your actual keys
# NOTE: The current keys in .env are TEST keys that only work in development
RECAPTCHA_SITEKEY=your-production-site-key-here
RECAPTCHA_SECRETKEY=your-production-secret-key-here
RECAPTCHA_USE_RECAPTCHA_DOMAIN=true

# Mail Configuration (Mailjet)
# Get your API credentials from https://app.mailjet.com/account/api_keys
MAIL_USER=your-mailjet-api-key
MAIL_PASS=your-mailjet-secret-key
MAIL_FROM=noreply@wiwi.tu-dresden.de
MAIL_RECEIVER=felix.reinsch@tu-dresden.de

# Backup Configuration
# Retention period for backups (in days)
BACKUP_RETENTION_DAYS=14

# Backup failure alerts
# Email address for backup failure notifications
ALERT_EMAIL=felix.reinsch@tu-dresden.de

# Optional: Webhook URL for Slack/Discord/Teams notifications
# Slack: https://api.slack.com/messaging/webhooks
# Discord: Server Settings → Integrations → Webhooks → Copy URL + /slack suffix
# Teams: Channel → Connectors → Incoming Webhook
ALERT_WEBHOOK_URL=
